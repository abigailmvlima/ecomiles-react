export enum ERoute {
  login = "/login",
  home = "/home",
  menu = "/menu",
  reward = "/reward",
  chat = "/chat",
  challenge = "/challenge",
  challengeDetail = "/challengeDetail",
}

const route = ERoute;
export default {
  route,
};

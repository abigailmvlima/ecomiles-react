export enum ERoute {
  login = "/login",
  home = "/home",
  menu = "/menu",
  reward = "/reward",
  chat = "/chat",
  challenge = "/challenge",
  challengeDetail = "/challengeDetail",
  challengeRegister = "/challengeRegister",
}

const route = ERoute;
export default {
  route,
};

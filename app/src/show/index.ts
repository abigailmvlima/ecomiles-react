export enum ERoute {
  login = "/login",
  home = "/home",
  menu = "/menu",
  register = "/register",
  reward = "/reward",
  chat = "/chat",
  challenge = "/challenge",
  challengeDetail = "/challengeDetail",
  challengeRegister = "/challengeRegister",
  userEdit = "/userEdit",
}

const route = ERoute;
export default {
  route,
};

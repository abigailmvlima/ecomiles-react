import svg from "assets/svg";
import styled from "styled-components";

export const Container = styled.div`
  display: flex;

`;

export const Logo = styled(svg.Heart)`
  display: flex;
  fill: #ffff;
  
`;
